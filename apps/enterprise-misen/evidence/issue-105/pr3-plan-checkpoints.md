# Issue #105 PR 3 受け入れ記録

## 変更前後

- 変更前（PR 2 完了時、依頼「8月の月次管理レポートを作成してください。」）: `pr2-after.png`
- 変更後（同じ依頼、上書きチェックポイントで待機中）: `pr3-after.jpg`
- 実ブラウザーで「このセッションでは同種を承認済みにする」を選び「承認」を押し、計画の全ステップ完了、回答、`output/8月月次管理レポート.xlsx` の成果物カードまで確認した。

## 削るもの

- [x] モデル選択なし
- [x] 設定画面なし
- [x] 生の推論・プロバイダー表示なし（接続先の provider / model は情報欄だけ）
- [x] 分岐なし
- [x] メッセージ編集なし
- [x] 再生成なし
- [x] 音声入力なし
- [x] Assistant Cloud なし
- [x] 添付は維持

## 受け入れ結果

- [x] ThreadStore に「計画」「ステップ進行」「チェックポイント要求・応答」のイベントを追加し、リデューサーテストで状態遷移を確認。
- [x] 実行前に3段階の計画を通知し、現在のステップを「進行中」で表示。
- [x] チェックポイントカードに操作内容・対象・リスク水準・理由、承認・拒否、同種のセッション承認を表示。
- [x] Issue #100 の語彙「動詞」「対象」「リスク水準（低・中・高）」「理由」「同一セッション限り」に合わせた。
- [x] 「毎回確認」は上書きと要素削除で応答を待ち、承認時だけ操作を続行。拒否時は Tool をブロック。
- [x] 同種承認後の同じ動詞は、そのセッション内で自動承認されることを自動テストで確認。
- [x] チェックポイント要求・応答を各 PC の追記専用 JSONL に記録。
- [x] 計画とチェックポイントの最終状態をローカル会話に保存し、旧 schemaVersion 1 を欠損なく読み込む移行テストを追加。
- [x] 成果物は既存の WorkspaceBoundary により `output/` のみ。実ブラウザーでも `output/8月月次管理レポート.xlsx` を確認。
- [x] セッション専用の別一時領域は現行実装に存在しない。WorkspaceBoundary の原子的書き込み用一時ファイルは既存テストで成功・失敗とも削除を確認。
- [x] `npm run test:unit`: 105 tests、102 pass、3 skip、0 fail。全体約12.6秒（30秒以内）。
- [x] `dist/web/assets`: `client.js` と `client.css` の2ファイル。
- [x] `package.json` / `package-lock.json` に変更なし。実行時依存は main と同一。
- [ ] 実 provider / 実 OfficeCLI を使った上書き・削除チェックポイントは未確認。ローカル注入 runner と実ブラウザーで確認。

## CI

GitHub checks の有無と結果は PR 作成後に確認し、報告する。
