# Decision 433-436 Pi Enterprise slice

Decisions 433-436 retain Misen as the authority for Capability (the fixed five-tool roster), Resource (realpath-contained workspace), Mutation (output-only atomic replacement), Secret/Network (provider-bound `OPENAI_API_KEY` and configured OpenAI endpoint), and high-impact side effects (absent; fail closed). Pi supplies the Agent loop, OpenAI Responses provider, and TypeBox tools but is not the security root.

The runtime-independent WorkspaceBoundary, Office Kit seam, synthetic data, formula policy, and atomic output design are carried forward from Decision 427/PR #71. DSH/Cordis composition, providers, session code, and UI are replaced by public Pi Agent/AI plus a restricted Node loopback server. The deterministic route uses Pi's public faux stream seam; the production UI and live runner use only Luna medium and Pi's official OpenAI provider with `maxRetries: 0`.

Normal execution is Node in-process, the selected workspace, and the configured OpenAI endpoint. There is no shell, subprocess, browser/Web/MCP tool, plugin, subagent, runtime download, or network monitor capability. The external observer is intentionally outside Agent tools. Live provider/device/EDR/TCP evidence remains a deployment gate, not a deterministic-pass claim.

## UI composition

The demo presentation uses `@assistant-ui/react@0.15.17` through its public External Store Runtime and Thread, Message, and Composer primitives. Misen owns the event store and adapts allowlisted Pi events over loopback-only SSE. Pi Agent `abort()` backs the same-seat Stop action. Assistant Cloud is not instantiated or configured, and raw reasoning/provider payloads never cross the server's UI event boundary. The styling follows the restrained DSH conversation grammar without importing DSH UI: right-aligned user bubble, bare assistant text, compact deterministic Tool rows folded after completion, sticky rounded Composer, optimistic user reconciliation, streaming, and bottom-aware scrolling.

## Current acceptance checkpoint

- Deterministic tests after Decision 438: 22 PASS, 0 FAIL, 1 explicit Windows symlink-fixture EPERM SKIP. The suite includes semantic output discovery, zero/multiple/non-xlsx rejection, pre-existing output immutability, sequential-turn process isolation, RFC 5987-safe artifact filename delivery, and rejection of a serialized malicious source workbook through the production create-output Tool.
- Mechanical July/August workbook Acceptance: PASS for all eight business axes, one output, and unchanged input hashes.
- Supply chain: clean `npm ci --ignore-scripts` PASS; the official-schema-validated CycloneDX 1.6 SBOM contains 247 resolved lockfile packages, including the development-only validator set; `npm audit --omit=dev --package-lock-only` reported 0 production vulnerabilities at the measurement time. The development-only esbuild package includes its Windows build executable; normal Agent work does not execute it.
- Historical pre-Decision-438 July Luna/medium live run: BLOCKED by the old independent output-name check. The Agent created exactly one file named `2024年7月_月次管理レポート.xlsx`; the old validator required `7月-月次管理レポート.xlsx`, while the business handoff specifies only the output directory and no exact filename representation. This historical result is not retroactively changed to PASS.
- Decision 438 changes only artifact discovery: Acceptance snapshots the authorized `output/` scope before the run, requires exactly one new regular `.xlsx` file, preserves any pre-existing outputs, and grades the actual new workbook through every existing business/security axis. It imposes no filename content or representation rule.
- Decision 438 fresh July run: FAIL — MONTH. The sole new authorized workbook was `月次管理レポート_7月.xlsx`; `Report!B2` was empty. A single observer-only diagnosis rerun, with model/prompt/Tools unchanged, again produced that sole filename and wrote `Report!B2 = "2024-07"` successfully. Company/value rows were `Alpha/1200/700`, `Beta/950/500`, `Gamma/1100/650`. The current Decision 429 period parser accepts only `7月` and `2024年7月`, while the business handoff does not specify a display representation. Decision 438 does not authorize adding ISO `YYYY-MM`; this is a new Acceptance/specification ambiguity, so August and rendered-browser acceptance remain NOT RUN pending human Decision.
- Diagnosis also observed general Spreadsheet Tool calls passing formula-looking strings (for example `=B5-C5`) rather than the Tool implementation's `{ formula: ... }` value form. Those calls reported success, but FORMULA/STATUS/TOTAL were not independently reached after MONTH failed. This is preserved as a possible general Tool-contract implementation finding, not silently repaired or credited as PASS.
- The July PID-scoped observer saw the Node runtime connect only to `162.159.140.245:443` (plus non-remote `0.0.0.0:0` rows); endpoint attribution must remain hostname/config based rather than treating that point-in-time IP as an architecture pin.

## Decision 439/440 checkpoint

- Decision 439's three browser preflight repairs are implemented deterministically: assistant-ui `Viewport` is the sole scroll owner, `ViewportFooter` measures the growing Composer, `ScrollToBottom` owns away-from-bottom visibility, the active run gets a quiet pre-activity placeholder, and a visible Brain final answer is no longer replaced by the controlled success fallback. Rendered-browser screenshots and interaction grading remain NOT RUN until live business parity.
- Decision 440 replaces the two-string period allowlist with strict normalization to `{ year, month }`, compared with the period independently derived from all three `Actuals!B4` source dates. `M月`, `YYYY年M月`, `YYYY-MM`, `YYYY-M`, `YYYY/MM`, `YYYY/M`, and real Date values are covered; wrong, malformed, ambiguous, and arbitrary text values fail.
- `spreadsheet_update.values` now advertises only `string | number | boolean | null | { formula: string }`. Formula objects reject additional properties such as `cachedValue`; runtime validation mirrors the schema. A plain string beginning with `=` remains a literal string, while structured formulas still pass the existing formula/external-reference guards.
- Clean deterministic verification at candidate `c686e8781393052ee6ef299bca3228c832088f29`: 24 PASS, 0 FAIL, 1 explicit Windows symlink EPERM SKIP; mechanical July/August Acceptance PASS; official CycloneDX 1.6 SBOM PASS with 247 lockfile packages; npm audit 0; diff check PASS.
- Fresh July Luna/medium live: PASS across SHEET, MONTH, ROWS, PROFIT_FORMULAS, STATUS, TOTAL, FOOTER, FORMAT, one output, and input hashes. Output was `7月_月次管理レポート.xlsx` (3215 bytes), elapsed 36536 ms, RSS 99229696 bytes, and all 23 Tool calls/results balanced. `Report!B2` was `2024-07`; Profit and Total updates used the new structured formula contract successfully.
- Fresh August Luna/medium live: FAIL at STATUS after MONTH and company/profit checks. Output was `月次管理レポート_8月.xlsx`; `Report!B2` was `2024-08`; company rows were correct. The Brain wrote safe structured `IF` formulas into E5:E7 with the correct company thresholds, while the current validator requires literal `On target` / `Review` values. The business handoff defines the status meaning but not literal-versus-formula representation, so changing Acceptance requires a human Decision. No retry or Acceptance change was made.
- July PID-scoped observation saw the Misen Node process connect to `172.66.0.243:443`, which resolved for configured `api.openai.com` at measurement time. A wrapper PowerShell connection was separately observed and not attributed to Agent authority. The August observer did not retain the Node descendant long enough to provide a valid sample and is recorded as unverified, not PASS.

## Decision 441 completion checkpoint

- Acceptance authority now follows the user request, `業務引継ぎ.md`, independently derived source/master facts, independently derived template preservation facts, Misen Security/Integrity, and explicit human Decisions. Ordinary business mismatches produce a complete eight-axis matrix instead of fail-fast exceptions.
- `SHEET` requires the usable report sheet but permits harmless extras. `ROWS` compares each discovered company with its Revenue/Cost association as an order-independent set and rejects duplicates, omissions, unknown replacements, and value swaps.
- The Acceptance-only formula verifier applies `safeFormula` first, uses a bounded pure parser/evaluator (no `eval`, `new Function`, subprocess, Office automation, LLM judgment, or cached values), constrains referenced cells, and checks Profit, Status, and Total with actual plus counterfactual/boundary inputs. Profit and Total still require safe local formula mechanisms; Status accepts either the correct literal or a semantically equivalent safe local formula.
- Footer, cell styles, row heights, and column widths are derived from the actual template and compared with the output rather than encoded as fixture constants. Regression negatives cover row association, literal Profit/Total, wrong arithmetic/dependencies/thresholds/branches/ranges, unsafe/external formulas, footer/style drift, and row/column dimension drift.
- Deterministic verification after the Decision 441 and rendered-browser repairs: 36 PASS, 0 FAIL, 1 explicit Windows symlink-fixture EPERM SKIP. Mechanical July and August each PASS all eight axes. `npm audit --omit=dev --package-lock-only` reports 0 vulnerabilities; the official CycloneDX 1.6 schema and negative control PASS with 328 resolved lockfile packages.
- Fresh July Luna/medium live: all eight axes PASS; output `output/月次管理レポート_7月.xlsx` (3215 bytes), 19 balanced Tool starts/results, elapsed 40962 ms, RSS 99319808 bytes, and unchanged inputs. PID-scoped observation found only the configured OpenAI HTTPS path (`172.66.0.243:443` at that observation), plus local process bookkeeping.
- The first August attempt ended in a provider-or-Tool fatal state whose original aggregate observer lacked enough classification evidence. That historical attempt remains recorded. After an observer-only correlation repair, the authorized fresh August rerun PASSed all eight axes; output `output/8月_月次管理レポート.xlsx` (3210 bytes), 18 balanced Tool starts/results, elapsed 32663 ms, RSS 98398208 bytes, and unchanged inputs. PID-scoped observation found only the configured OpenAI HTTPS path (`162.159.140.245:443` at that observation). Point-in-time IPs are evidence, not an endpoint pin.
- Rendered-browser Acceptance uses assistant-ui as presentation-only. The final repairs remove the transient duplicate optimistic message, suppress Tool-result JSON from Assistant output, carry the independently validated artifact in the terminal event, constrain `ThreadPrimitive.Viewport` as the sole page scroll owner, and position back-to-bottom relative to the measured Composer footer. Eight-line Composer growth produced no content/control overlap; scrolling away from bottom remained user-controlled while events arrived, and the control returned to the live bottom.
- A real Luna response exposed Markdown syntax, so the official `@assistant-ui/react-markdown@0.14.13` renderer was added as a bounded presentation dependency. Brain-authored links and images render as inert text, preserving the no-browser-network/no-navigation authority. New, Running, and Completed screenshots are stored under `evidence/ui/`; the Completed state keeps the actual Brain answer, folds 20 operations, and exposes only the validated `.xlsx` download.
- Browser New/Running/Completed/Artifact Acceptance PASSed on the final code: quiet centered new session, one immediate optimistic user bubble, run-scoped `考えています…`, subtle Tool rows with no raw reasoning or Tool JSON, same-seat Stop, folded completion, safe Markdown, compact artifact, and successful download action. Raw reasoning/provider payloads and the API key were neither collected nor exposed.
