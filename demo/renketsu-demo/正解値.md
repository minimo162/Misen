# 連結デモの正解値

正本は [`validation/正解値.md`](validation/正解値.md) と JSON fixture です。生成器が作る `validation/extracted.correct.json` は提出元単位の抽出値、`validation/expected.json` はレート適用後の百万円 JPY 台帳値・合計を収録します。

```powershell
node demo/renketsu-demo/validation/verify-expectations.mjs
```
