# Validation run history

| Run | Reached | Result / cause | Fix before next run |
| --- | --- | --- | --- |
| 1 `run-mt968erb-83izr` | 0 model turns; bootstrap only | Failed: persisted old session kept the pre-change system prompt and input verification stopped at position 114. | `Record-Demo.ps1` and manual validation now create a fresh session through `POST /api/sessions`. |
| 2 `run-mt96wjs2-pm2r7` | 11 model decisions; 9 host-tool successes | Failed: after successful report/xlsx reads, the model repeatedly read `Update-Ledger.ps1`, never wrote extraction JSON, then falsely answered that host tools were unavailable. | Strengthen `config.demo.json`: prohibit reading tool sources/ledger, force wildcard Read-Xlsx once, immediate extraction write, and forbid false unavailable answers after successful tool results. |
| 3 `run-mt97dc2c-4j5r3` | 9 model decisions; 7 host-tool successes; input retry used | Failed: the strengthened system prompt was present, but the generic work-mode protocol contradicted it by telling the model to obtain approval before arbitrary commands; the model ended by asking permission and never wrote extraction JSON. | Make the protocol reflect `autoApprove.command=true`, explicitly forbidding an approval answer when command execution is already auto-approved; add smoke coverage. |
| 4 `run-mt97ld01-tafdi` | 7 model decisions; 6 host-tool successes; input retry used | Failed: the model again read the ledger and tool source, then claimed missing host access. Diagnosis found that work-mode prompt composition discarded all system-role messages, so none of the strengthened demo instructions reached Copilot. | Include the active session/config system instructions in every work-mode Copilot prompt and add a sentinel smoke assertion. |
| 5 `run-mt97xj63-5o2n0` | 3 model decisions; 2 host-tool successes; input retry used | Failed safely after a malformed follow-up response. The model requested `patterns:[\"reports/\",\"rates/\"]`; `read_files` interpreted these as exact file globs and returned no matches, depriving the next turn of report evidence. | Normalize trailing-slash read_files patterns to the documented immediate-content form (`directory/*`) and add smoke coverage. |
| 6 `run-mt983tm8-o458p` | 3 model decisions; 2 host-tool successes; input retry used | Failed safely after a second malformed follow-up even though directory patterns now returned all report evidence. The checkpoint showed that `/retry` reused a session containing the first input-mismatch error, polluting the retry history. | Keep one retry per take, but create a second fresh session and submit a clean `/api/turn`; never reuse the failed session through `/retry`. |
| 7 `run-mt98gcuu-smx4n` | 3 model decisions; 2 host-tool successes; input retry used in a fresh session | Failed safely after `read_files`. Clipboard evidence showed Copilot emitted an otherwise correct Read-Xlsx tool call with literal single backslashes inside the JSON command string, making the JSON invalid twice. | Use forward slashes in every system-prompt command and tool-call example so Copilot can emit valid JSON without backslash escaping; the host normalizer still canonicalizes the known command before execution. |
| 8 `run-mt98p3xr-uwsvm` | 4 model decisions; 3 host-tool successes; no input retry | Reached and successfully executed Read-Xlsx, then failed to preserve the write_file tool envelope. Edge diagnostics showed two copy candidates on the long response, consistent with the instructed fenced JSON creating a code-copy button in addition to the response-copy button. | Require the already-supported plain `CONTENT:` payload and explicitly forbid Markdown code fences, leaving a single response-copy target for the Edge bridge. |
| 9 `run-mt98y3vf-gz71g` | 7 model decisions; 5 host-tool successes; input retry used in a fresh session | Reached Read-Xlsx, write_file, and Update-Ledger. In the clean retry session the model treated turn-0 list_files as if report contents had been read, skipped read_files, wrote an empty companies array, then failed closed on omitted OS15 and repeated the write. | State that turn-0 list_files is names-only, require host.read_files as the first model action before Read-Xlsx, and add the exact initial read_files response shape. |
| 10 `run-mt9982xr-qits6` | 4 model decisions; 3 host-tool successes; input retry used in a fresh session | Followed the required read_files and Read-Xlsx sequence, but response recovery began while the Copilot UI still exposed `生成を停止する`. No response-copy control appeared in three attempts, innerText fallback was not a protocol response, and the run stopped safely before any write. | Budget exhausted. The remaining likely repair is to make Edge completion detection wait for generation controls to disappear before copying; this touches the otherwise-frozen real Copilot connection path and was not attempted. |
