# Validation run history

| Run | Reached | Result / cause | Fix before next run |
| --- | --- | --- | --- |
| 1 `run-mt968erb-83izr` | 0 model turns; bootstrap only | Failed: persisted old session kept the pre-change system prompt and input verification stopped at position 114. | `Record-Demo.ps1` and manual validation now create a fresh session through `POST /api/sessions`. |
| 2 `run-mt96wjs2-pm2r7` | 11 model decisions; 9 host-tool successes | Failed: after successful report/xlsx reads, the model repeatedly read `Update-Ledger.ps1`, never wrote extraction JSON, then falsely answered that host tools were unavailable. | Strengthen `config.demo.json`: prohibit reading tool sources/ledger, force wildcard Read-Xlsx once, immediate extraction write, and forbid false unavailable answers after successful tool results. |
| 3 `run-mt97dc2c-4j5r3` | 9 model decisions; 7 host-tool successes; input retry used | Failed: the strengthened system prompt was present, but the generic work-mode protocol contradicted it by telling the model to obtain approval before arbitrary commands; the model ended by asking permission and never wrote extraction JSON. | Make the protocol reflect `autoApprove.command=true`, explicitly forbidding an approval answer when command execution is already auto-approved; add smoke coverage. |
| 4 `run-mt97ld01-tafdi` | 7 model decisions; 6 host-tool successes; input retry used | Failed: the model again read the ledger and tool source, then claimed missing host access. Diagnosis found that work-mode prompt composition discarded all system-role messages, so none of the strengthened demo instructions reached Copilot. | Include the active session/config system instructions in every work-mode Copilot prompt and add a sentinel smoke assertion. |
| 5 `run-mt97xj63-5o2n0` | 3 model decisions; 2 host-tool successes; input retry used | Failed safely after a malformed follow-up response. The model requested `patterns:[\"reports/\",\"rates/\"]`; `read_files` interpreted these as exact file globs and returned no matches, depriving the next turn of report evidence. | Normalize trailing-slash read_files patterns to the documented immediate-content form (`directory/*`) and add smoke coverage. |
| 6 `run-mt983tm8-o458p` | 3 model decisions; 2 host-tool successes; input retry used | Failed safely after a second malformed follow-up even though directory patterns now returned all report evidence. The checkpoint showed that `/retry` reused a session containing the first input-mismatch error, polluting the retry history. | Keep one retry per take, but create a second fresh session and submit a clean `/api/turn`; never reuse the failed session through `/retry`. |
| 7 `run-mt98gcuu-smx4n` | 3 model decisions; 2 host-tool successes; input retry used in a fresh session | Failed safely after `read_files`. Clipboard evidence showed Copilot emitted an otherwise correct Read-Xlsx tool call with literal single backslashes inside the JSON command string, making the JSON invalid twice. | Use forward slashes in every system-prompt command and tool-call example so Copilot can emit valid JSON without backslash escaping; the host normalizer still canonicalizes the known command before execution. |
| 8 `run-mt98p3xr-uwsvm` | 4 model decisions; 3 host-tool successes; no input retry | Reached and successfully executed Read-Xlsx, then failed to preserve the write_file tool envelope. Edge diagnostics showed two copy candidates on the long response, consistent with the instructed fenced JSON creating a code-copy button in addition to the response-copy button. | Require the already-supported plain `CONTENT:` payload and explicitly forbid Markdown code fences, leaving a single response-copy target for the Edge bridge. |
| 9 `run-mt98y3vf-gz71g` | 7 model decisions; 5 host-tool successes; input retry used in a fresh session | Reached Read-Xlsx, write_file, and Update-Ledger. In the clean retry session the model treated turn-0 list_files as if report contents had been read, skipped read_files, wrote an empty companies array, then failed closed on omitted OS15 and repeated the write. | State that turn-0 list_files is names-only, require host.read_files as the first model action before Read-Xlsx, and add the exact initial read_files response shape. |
| 10 `run-mt9982xr-qits6` | 4 model decisions; 3 host-tool successes; input retry used in a fresh session | Followed the required read_files and Read-Xlsx sequence, but response recovery began while the Copilot UI still exposed `生成を停止する`. No response-copy control appeared in three attempts, innerText fallback was not a protocol response, and the run stopped safely before any write. | Budget exhausted. The remaining likely repair is to make Edge completion detection wait for generation controls to disappear before copying; this touches the otherwise-frozen real Copilot connection path and was not attempted. |
| 11 `run-mt9azi0e-zpbaf` | 1 model decision; turn-0 list_files only; input retry used in a fresh session | Failed closed after 120 seconds. The completed latest response and enabled `応答のコピー` button were both present, but the response toolbar was about ten ancestors above `markdown-reply`; the six-level scoped search never associated the copy control with that response. No workspace output was written. | Treat the live M365 `role="article"` + `fai-CopilotMessage` container (and its message wrapper) as a first-class response root, keeping the latest-response and three-condition completion gates intact. |
| 12 `run-mt9bswyy-xf8f6` | 5 model decisions; 5 host executions; input retry used in a fresh session | Reached read_files, Read-Xlsx, a full extraction write, and Update-Ledger. The PowerShell tool then failed because a one-item JSON array was enumerated into a scalar by the `Get-RenketsuArray` function, so StrictMode rejected `.Count`; the model's attempted repair overwrote the extraction with empty arrays. The validation driver also hit Node fetch's default headers timeout after the server-side run had progressed. | Return arrays with `Write-Output -NoEnumerate`, add a single-item-array diagnostic that requires the intended schema error, and use `http.request` without the fetch headers deadline in the local validation driver. |
| 13 `run-mt9c6zig-bj2re` | 4 model decisions; 4 host executions; no input retry | Reached all four fixed host actions, but every company contained only one revenue quote instead of five metric quotes. Update-Ledger rejected `JP01.quotes[]` with the intended schema error; the run was canceled before a repair overwrite. It contained all 19 company IDs and OS16 but also over-reported unit/account issues. | Make the fixed extraction contract explicit: five ordered exact quotes per company (95 total), deterministic company-name mapping, xlsx label-cell quote semantics, and exactly the four designed issue records. |
| 14 `run-mt9ci0ko-mg8q6` | 6 model decisions; 4 host executions; input retry used in a fresh session | Completed all host actions and saved the ledger with exact totals (revenue 670552.0, operatingProfit 80582.7, netIncome 52761.1, totalAssets 1120599.0, employees 14350), 19 processed, OS16 missing, 95 quotes, 4 issues, and 5 confirmations. Exact extraction comparison still failed on five strings only: four shortened issue messages and the shortened OS16 missing quote. | Superseded: judge free prose by target-linked, fact-consistent meaning while retaining strict structural and numeric checks; do not pin reference prose in the prompt. |

| 15 `run-mt9dw7tg-oty43` | 6 model decisions; read, xlsx, write, and ledger update reached after the allowed fresh input retry | Failed: every response-copy click was followed by a `Runtime.evaluate` clipboard-read timeout, so recovery fell back to innerText; the model wrote only one company, Update-Ledger rejected the omission, and the repair repeated the partial write. | Read the copied response from the Windows clipboard first, retaining the browser clipboard and innerText paths as fallbacks; add smoke coverage. |
| 16 `run-mt9eb534-y1bdd` | 4 model decisions; 4 host executions; allowed fresh input retry used | Completed all business actions with exact totals, 19 submitted companies, OS16 missing, 95 quotes, 4 correctly classified issues, and 5 confirmations. Semantic acceptance failed because four issue messages and the missing quote did not themselves name their target. | No prompt prose pinning. Preserve the literal semantic gate and use the final permitted fresh run to test natural output variance. |
| 17 `run-mt9eeor2-dpm83` | 4 model decisions; 4 host executions; no input retry | Completed all business actions with exact totals and counts again. The same five target-less free-text values recurred, so semantic acceptance failed. | Three-run budget exhausted; no recording or edited video was started. |
| 18 `run-mt9gaewd-4pmbj` / retry `run-mt9gbguc-hy4r0` | Initial fresh session hit the known input-position mismatch after turn-0 list_files; the allowed fresh input retry completed 4 model decisions and all 4 host executions. | PASS under the revised acceptance contract: 19 submitted companies, OS16 missing, all 95 quotes, all 4 issue type/field classifications, all metric values, and non-empty non-contradictory issue messages matched. Update-Ledger also completed successfully. | Validation objective achieved. Proceeded to the single recording take. |

Acceptance note: issue targets and missing companies are identified by exact structured IDs; issue messages must be non-empty and non-contradictory, while missing quotes may be empty or natural fixed prose because no source report exists. All values, submitted/missing classification, issue count/type/field, and quote counts remain exact.

Recording note: an earlier Codex-owned capture attempt stopped before instruction submission after `gdigrab`, LIMITED scheduled-task capture, `ddagrab`, and `CopyFromScreen` all failed their separated qualification. The subsequent human-started take `manual-take.mp4` completed with capture mode `ffmpeg`; instruction sent at `2026-08-26T12:01:58.6080855+09:00`, saved at `2026-08-26T12:03:35.3003514+09:00`, elapsed 96.692 seconds (1.61 minutes), session `mt9icyqzvay6`, run `run-mt9icyrl-nzjcv`, one fresh input retry used. Extraction freshness, ledger freshness, exact truth comparison, ledger totals, write event, ledger event, ffmpeg, and recording-file checks were all true. The metadata status remains `applied_unverified`, so it is not described as a separate real-machine Excel verification.

Video note: HyperFrames produced `demo/video/output/デモ紹介_v1.mp4` from that take. The output is 62.000 seconds, 1920 x 1080, 30fps, 1,860 frames, H.264/BT.709 SDR, with no audio stream because the source recording was silent. The final QA passed duration, typography/mojibake, black-frame, scene-boundary, card-hold, and integrity checks; full evidence is in `demo/video/qa/QA-REPORT.md`.

Human-QA correction: the prior video pass is superseded. At raw time 0 seconds (12:01 on the taskbar), the visible Edge tab was already on `conversation/dd764fbb...` and showed the exact final answer stored for session `mt9gbgtilhj0`, run `run-mt9gbguc-hy4r0` (11:05:02–11:06:32). It stayed on that old answer while the recorded run actually executed in fresh session `mt9icyqzvay6`, run `run-mt9icyrl-nzjcv` (12:02:11–12:03:35). Root cause: `Record-Demo.ps1` selected the newest visible Edge window without binding it to the copilot-edge-owned process or fresh session. The new driver binds the exact Edge PID, stamps and reads a session marker from the displayed DOM before capture, passes `sessionId` explicitly to `/api/turn`, and requires the visible response count to grow during the run. The new two-second frame-difference gate rejects both the old raw footage and its final live-footage ROI.
